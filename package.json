{
  "name": "core_framework",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
      "init": "node run-project.js && docker compose up -d --build",
      "dev": "cd Backoffice/theme && npm run dev"
    }
  }
  